import { TriProduitsPipe } from './tri-produits.pipe';

describe('TriProduitsPipe', () => {
  it('create an instance', () => {
    const pipe = new TriProduitsPipe();
    expect(pipe).toBeTruthy();
  });
});

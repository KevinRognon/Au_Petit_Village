import { PrixcroissantPipe } from './prixcroissant.pipe';

describe('PrixcroissantPipe', () => {
  it('create an instance', () => {
    const pipe = new PrixcroissantPipe();
    expect(pipe).toBeTruthy();
  });
});
